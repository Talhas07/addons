<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Wizard View for Creating Invoices -->
    <record id="view_repair_make_invoice" model="ir.ui.view">
        <field name="name">repair.make.invoice.form</field>
        <field name="model">repair.make.invoice</field>
        <field name="arch" type="xml">
            <form string="Create Invoices">
                <group>
                    <field name="group"/>
                </group>
                <footer>
                    <button name="make_invoices" string="Create Invoices" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_repair_make_invoice" model="ir.actions.act_window">
        <field name="name">Create Invoices</field>
        <field name="res_model">repair.make.invoice</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="repair.model_repair_order"/>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
